<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--   color for the app bar and other primary UI elements -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--   a darker variant of the primary color, used for
           the status bar (on Android 5.0+) and contextual app bars -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--   a secondary color for controls like checkboxes and text fields -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/main_black</item>
    </style>

    <style name="TextInputLayoutDefault" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:textColorHint">@color/white</item>
        <item name="boxStrokeColor">@color/colorAccent</item>
        <item name="hintTextColor">@color/colorAccent</item>
        <item name="helperTextTextColor">@color/white</item>
    </style>

    <style name="EditTextDefault">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/value_sixteen_sp</item>
    </style>

    <style name="ButtonDefault">
        <item name="android:textSize">@dimen/value_eighteen_sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="textAllCaps">false</item>
        <item name="android:background">@drawable/selector_button_default</item>
        <item name="android:layout_margin">@dimen/margin_default_button</item>
    </style>

    <style name="BackButtonDefault">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_marginTop">@dimen/value_sixteen_dp</item>
        <item name="android:layout_marginStart">@dimen/value_sixteen_dp</item>
        <item name="srcCompat">@drawable/ic_keyboard_arrow_left_24px</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
    </style>

    <style name="TitleDefault">
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/title_size_default</item>
    </style>
</resources>